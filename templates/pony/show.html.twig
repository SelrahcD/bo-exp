{% extends 'base.html.twig' %}

{% block title %}Pony{% endblock %}

{% block body %}
    <h1>Pony</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pony.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pony.name }}</td>
            </tr>
            <tr>
                <th>Birthdate</th>
                <td>{{ pony.birthdate ? pony.birthdate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pony_index') }}">back to list</a>

    <a href="{{ path('pony_edit', {'id': pony.id}) }}">edit</a>

    {{ include('pony/_delete_form.html.twig') }}
{% endblock %}
